<Window x:Class="CompHomp.SalesStatisticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="Статистика продаж сборок ПК" 
        Height="700" Width="1200"
        Background="#FF1E1E1E"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        
        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#FF2C2C2C"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#20FFFFFF" BlurRadius="15" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FilterButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernListViewStyle" TargetType="ListView">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListView">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer Focusable="false" 
                                          HorizontalScrollBarVisibility="Disabled" 
                                          VerticalScrollBarVisibility="Auto">
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="SubHeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="Foreground" Value="#FFA0A0A0"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FF252525" Padding="20" BorderThickness="0,0,0,1" BorderBrush="#FF3C3C3C">
            <StackPanel>
                <TextBlock Text="Статистика продаж сборок ПК" 
                           Style="{StaticResource HeaderTextStyle}"
                           FontSize="20"
                           Margin="0,0,0,15"/>
                
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Период:" 
                               Foreground="White"
                               VerticalAlignment="Center" 
                               Margin="0,0,10,0"/>
                    <DatePicker x:Name="StartDatePicker" 
                                Width="150" 
                                Margin="0,0,10,0" 
                                SelectedDate="{Binding StartDate}"/>
                    <TextBlock Text="—" 
                               Foreground="White"
                               VerticalAlignment="Center" 
                               Margin="0,0,10,0"/>
                    <DatePicker x:Name="EndDatePicker" 
                                Width="150" 
                                Margin="0,0,10,0" 
                                SelectedDate="{Binding EndDate}"/>
                    <Button x:Name="RefreshButton" 
                            Content="Обновить" 
                            Click="RefreshButton_Click"
                            Style="{StaticResource FilterButtonStyle}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1">
            <StackPanel>
                <Border Style="{StaticResource StatCardStyle}">
                    <StackPanel Margin="15">
                        <TextBlock Text="Статистика продаж" 
                                   Style="{StaticResource HeaderTextStyle}"
                                   Margin="0,0,0,10"/>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            

                            <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                                <TextBlock Text="Общая выручка" 
                                           Style="{StaticResource SubHeaderTextStyle}"/>
                                <TextBlock x:Name="TotalRevenueTextBlock" 
                                           Text="0 ₽" 
                                           FontSize="18" 
                                           FontWeight="Bold" 
                                           Foreground="#FF2ECC71"/>
                            </StackPanel>
                            

                            <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                                <TextBlock Text="Количество продаж" 
                                           Style="{StaticResource SubHeaderTextStyle}"/>
                                <TextBlock x:Name="TotalSalesCountTextBlock" 
                                           Text="0" 
                                           FontSize="18" 
                                           FontWeight="Bold" 
                                           Foreground="#FF3498DB"/>
                            </StackPanel>
                            

                            <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                                <TextBlock Text="Средний чек" 
                                           Style="{StaticResource SubHeaderTextStyle}"/>
                                <TextBlock x:Name="AverageOrderValueTextBlock" 
                                           Text="0 ₽" 
                                           FontSize="18" 
                                           FontWeight="Bold" 
                                           Foreground="#FF9B59B6"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource StatCardStyle}" Margin="0,10,0,0">
                    <StackPanel Margin="15">
                        <TextBlock Text="Графики продаж" 
                                   Style="{StaticResource HeaderTextStyle}"
                                   Margin="0,0,0,10"/>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <lvc:CartesianChart x:Name="SalesLineChart" 
                                                Grid.Column="0" 
                                                Height="250">
                                <lvc:CartesianChart.AxisX>
                                    <lvc:Axis Title="Дата" 
                                             Foreground="White"
                                             Labels="{Binding XAxisLabels}">
                                    </lvc:Axis>
                                </lvc:CartesianChart.AxisX>
                                <lvc:CartesianChart.AxisY>
                                    <lvc:Axis Title="Выручка (₽)" 
                                             Foreground="White"
                                             LabelFormatter="{Binding YAxisFormatter}">
                                    </lvc:Axis>
                                </lvc:CartesianChart.AxisY>
                            </lvc:CartesianChart>

                            <lvc:PieChart x:Name="BuildCategoryPieChart" 
                                          Grid.Column="1" 
                                          Height="250"
                                          LegendLocation="Right">
                                <lvc:PieChart.ChartLegend>
                                    <lvc:DefaultLegend Foreground="White"/>
                                </lvc:PieChart.ChartLegend>
                            </lvc:PieChart>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource StatCardStyle}" Margin="0,10,0,0">
                    <StackPanel Margin="15">
                        <TextBlock Text="Топ сборок" 
                                   Style="{StaticResource HeaderTextStyle}"/>
                        
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            

                            <ListView x:Name="TopBuildsListView" 
                                      Grid.Row="0" 
                                      Grid.Column="0" 
                                      Style="{StaticResource ModernListViewStyle}">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Name}" 
                                                       Margin="0,0,10,0" 
                                                       FontWeight="Bold"
                                                       Foreground="White"/>
                                            <TextBlock Text="{Binding SalesCount, StringFormat='Продано: {0}'}" 
                                                       Foreground="#FFA0A0A0"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>

                            <TextBlock x:Name="NoBuildsTextBlock" 
                                       Grid.Row="0" 
                                       Grid.Column="0" 
                                       Text="Пока нет проданных сборок"
                                       Foreground="#FFA0A0A0"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Visibility="Collapsed"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
